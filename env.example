# Database Configuration
# Replace with your PostgreSQL connection string
DATABASE_URL="postgresql://postgres:12345@localhost:5432/health_tracker"

# NextAuth Configuration
# Generate a secure secret: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="http://localhost:3000"

# Optional: JWT Secret (if using custom JWT)
JWT_SECRET="your-jwt-secret-here"

# Optional: Database Pool Configuration
# DATABASE_POOL_MIN=2
# DATABASE_POOL_MAX=10

# Optional: API Rate Limiting
# RATE_LIMIT_MAX=100
# RATE_LIMIT_WINDOW_MS=900000

# Optional: ESP32 Device Authentication
# ESP32_API_KEY="your-esp32-api-key"

# Optional: Email Configuration (for notifications)
# EMAIL_SERVER_HOST="smtp.gmail.com"
# EMAIL_SERVER_PORT=587
# EMAIL_SERVER_USER="your-email@gmail.com"
# EMAIL_SERVER_PASSWORD="your-app-password"

# Optional: Redis Configuration (for caching)
# REDIS_URL="redis://localhost:6379"

# Optional: Analytics (Google Analytics)
# GA_TRACKING_ID="G-XXXXXXXXXX"

# Optional: Sentry (Error Tracking)
# SENTRY_DSN="https://your-sentry-dsn@sentry.io/project-id"

# Development Settings
NODE_ENV="development" 